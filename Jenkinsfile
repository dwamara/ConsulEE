pipeline {
  agent any
  stages {
    stage('checkout') {
      steps {
        git(url: 'https://github.com/dwamara/ConsulSDREE.git', branch: 'dev')
      },
    stage('build') {
      steps {
      }        
    }
  }
}
